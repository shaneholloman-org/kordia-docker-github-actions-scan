---
name: Reusing Superlint Code Base Standard

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  workflow_call:
    name: Reference Federated Super Linter
    uses: shaneholloman/kordia-super-linter/.github/workflows/superlint-code-base-custom.yaml@main
          
